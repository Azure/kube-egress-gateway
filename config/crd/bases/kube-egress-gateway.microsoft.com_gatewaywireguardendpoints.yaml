---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: gatewaywireguardendpoints.kube-egress-gateway.microsoft.com
spec:
  group: kube-egress-gateway.microsoft.com
  names:
    kind: GatewayWireguardEndpoint
    listKind: GatewayWireguardEndpointList
    plural: gatewaywireguardendpoints
    singular: gatewaywireguardendpoint
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GatewayWireguardEndpoint is the Schema for the gatewaywireguardendpoints
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: GatewayWireguardEndpointSpec defines the desired state of
              GatewayWireguardEndpoint
            properties:
              gwPrivateIpAddress:
                description: The private IP assigned to the gateway node.
                type: string
              nodeName:
                description: Name of the node to this GatewayWireguardEndpoint belongs
                  to.
                type: string
              staticGatewayConfiguration:
                description: Name of the StaticGatewayConfiguration resource this
                  GatewayWireguardEndpoint belongs to.
                type: string
              wireguardKeySecret:
                description: Name of the secret containing wireguard keypair information.
                type: string
            type: object
          status:
            description: GatewayWireguardEndpointStatus defines the observed state
              of GatewayWireguardEndpoint
            properties:
              message:
                description: Additional message (e.g. error) to explain the state.
                type: string
              state:
                description: State of the endpoint.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
