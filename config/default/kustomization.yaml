# Adds namespace to all resources.
namespace: kube-egress-gateway-system

# Value of this field is prepended to the
# names of all resources, e.g. a deployment named
# "wordpress" becomes "alices-wordpress".
# Note that it should also match with the prefix (text before '-') of the namespace
# field above.
namePrefix: kube-egress-gateway-

# Labels to add to all resources and selectors.
#commonLabels:
#  someName: someValue

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../crd
- ../rbac
- ../resource
- ../azureconfig
- ../manager
- ../daemon
- ../cnimanager
images:
- name: cni
  newName: shafanreg.azurecr.io/kubeegress/kube-egress-gateway-cni
  newTag: 433102c
- name: cnimanager
  newName: shafanreg.azurecr.io/kubeegress/kube-egress-gateway-cnimanager
  newTag: 433102c
- name: controller
  newName: shafanreg.azurecr.io/kubeegress/kube-egress-gateway-controller
  newTag: 433102c
- name: daemon
  newName: shafanreg.azurecr.io/kubeegress/kube-egress-gateway-daemon
  newTag: 433102c
